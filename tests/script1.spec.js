
/*
const {test,exp //sanity : newly added


ect} = require("@playwright/test")
const exp = require("constants")
const { inflate } = require("zlib")

 test("to check",async({page})=>{

 await page.goto("https://shopnewage.com/")

 const newpage = await page.context().newPage()
 
await expect()

await expect.soft(page)

 })
 info4={

    name:"mayuri"
 }

 for(let prop in info4)
 {

console.log(prop,info4[prop])

 }

 for(let key in info4)


 console.log(key,info40[key])
 //splice()

 //smoke : critical functionality

*/

 let xyz = {

    name : "mayuri",
    lastname : "beldar",
    age : 4,
    number : 444

 }
 console.log(xyz)


 for(let key in xyz)
 {
    console.log(key,xyz[key])
 }

 //smoke//critical//
 //sanity//new

